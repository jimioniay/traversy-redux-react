const initialState = {
  completed: false,
  data: {},
};
const addPost = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default addPost;
